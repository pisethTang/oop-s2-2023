#include <iostream>
#include "University.h"
#include "Course.h"
#include "Student.h"
#include "Gradebook.h"
#include "Grade.h"
// #include "Person.h"
#include "Instructor.h"

int main(){

// Create a university
    University u1("University of Adelaide", "Adelaide North Terrace", 10); // University has a maximum of 10 courses
    // std::cout << u1.getName() << std::endl;
    // std::cout << u1.getLocation() << std::endl;	
    // u1.setName("University of Adelaide");
    // u1.setLocation("Adelaide, North Terrace");

// Add a course to this university
    // Course c1("Theory of Computation", 1, 10);
    // u1.addCourse(c1.getCourseID(), c1.getCourseName());
// Add a student to this course
    

// Add a grade for this student for some of the assignments


    return 0;
}